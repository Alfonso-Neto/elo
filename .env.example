# These are the only public runtime values currently consumed by the browser.
# Use a publishable/anon key only. Never put a service-role or AI secret here.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
