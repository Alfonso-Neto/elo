# These are the only public runtime values currently consumed by the browser.
# Use an sb_publishable_* key only. Never put a legacy key, user token,
# service-role key or AI secret here.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
